# Hub stack — copy to `.env` beside `hub.yml` when overriding defaults.

# Optional: Cloudflare tunnel token for token-based `cloudflared run` (M0.5).
# CLOUDFLARE_TUNNEL_TOKEN=

# NATS — JetStream enabled in compose command line.
NATS_SERVER_NAME=terra-hub-nats
