{
  "feature": "M0.4 Docker Compose vault skeleton — caddy heartbeat, r2-pull stub, profiles, compose smoke",
  "branch": "feature/docker-compose-vault-skeleton",
  "agents": ["orch", "test-agent-fast-ci"],
  "tasks": {
    "orch": [
      "Land M0.4; merge-open-PR-first discipline",
      "Commit subjects end with (#8)"
    ],
    "test-agent-fast-ci": [
      "compose_vault integration smoke; tooling excludes compose_hub and compose_vault"
    ]
  },
  "approved_deletions": [],
  "acceptance_criteria": [
    "deploy/compose/vault.yml + vault.override.dev.yml + vault.override.ci.yml",
    "deploy/caddy/Caddyfile.vault JSON on GET /",
    "deploy/workers/r2-pull Dockerfile stub (sleep loop + /var/lib/vault layout)",
    "Makefile compose-vault; CI compose-smoke runs hub + vault integration tests",
    "minimal profile mem_limit sum ≤ 480 MB",
    "00-index M0.4 [x]; memory/decisions entry"
  ]
}
