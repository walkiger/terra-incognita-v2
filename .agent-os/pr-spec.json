{
  "feature": "M0.2 Python baseline — uv lockfile, ruff, mypy strict, pyproject (replace pip requirements-ci)",
  "branch": "chore/python-pyproject-baseline",
  "agents": ["orch", "backend-implementation-agent", "test-agent-fast-ci"],
  "tasks": {
    "orch": [
      "Land M0.2; keep merge-open-PR-first discipline",
      "Commit subjects end with (#6)"
    ],
    "backend-implementation-agent": [
      "Keep ti_hub mypy-clean under strict settings"
    ],
    "test-agent-fast-ci": [
      "Maintain tests/test_tooling.py + DB tests + layout tests green under uv CI"
    ]
  },
  "approved_deletions": ["pytest.ini", "requirements-ci.txt"],
  "acceptance_criteria": [
    "CI: uv sync --frozen --extra dev + ruff check/format + mypy ti_hub + pytest",
    "make bootstrap/test/fmt/lint use uv",
    "uv.lock committed; no root requirements-ci.txt",
    "app/engine/pyproject.toml stub present for separate engine package path"
  ]
}
