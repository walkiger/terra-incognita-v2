# `app/backend/` — Hub backend (FastAPI in M5)

**Today:** Python package **`ti_hub`** with **`ti_hub.db`** SQLite persistence (phase **M1**).

Run tests from repo root:

```bash
py -m pip install -r requirements-ci.txt
py -m pytest tests/db -q
```
