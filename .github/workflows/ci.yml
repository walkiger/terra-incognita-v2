name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.12"
      - run: uv sync --frozen --extra dev
      - run: uv run ruff check app/backend/api app/backend/models app/backend/ti_hub tests
      - run: uv run ruff format --check app/backend/api app/backend/models app/backend/ti_hub tests

  type-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.12"
      - run: uv sync --frozen --extra dev
      - run: uv run mypy app/backend/ti_hub app/backend/api app/backend/models
      - run: uv run mypy app/engine/terra_engine

  migration-roundtrip-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.12"
      - run: uv sync --frozen --extra dev
      - name: Alembic migrations vs canonical SQL
        run: uv run pytest tests/db/test_alembic_migrations.py -q

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.12"
      - run: uv sync --frozen --extra dev
      - name: Pytest (excludes compose_* markers)
        run: .venv/bin/python -m pytest tests -q -m "not compose_hub and not compose_vault and not compose_observability and not compose_litestream and not compose_r2_pull and not alembic_isolation"

  schema-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.12"
      - run: uv sync --frozen --extra dev
      - run: uv run python scripts/schema_lint.py

  protected-deletions:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Fetch base for comparison
        run: git fetch origin main:refs/remotes/origin/main --quiet || true
      - run: python scripts/check_protected_deletions.py
        env:
          GITHUB_ACTIONS: "true"

  secrets-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.12"
      - run: uv sync --frozen --extra dev
      - name: Validate SOPS metadata files exist
        run: uv run pytest tests/test_secrets_layout.py -q

  pre-commit-ci:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Fetch base branch for protected-deletions hook
        run: |
          base="${GITHUB_BASE_REF:-main}"
          git fetch origin "${base}:refs/remotes/origin/${base}" --quiet || true
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.12"
      - run: uv sync --frozen --extra dev
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Run pre-commit (PR = diff-only; push to main = full tree)
        run: |
          if [ "${GITHUB_EVENT_NAME}" = "pull_request" ]; then
            uv run pre-commit run --from-ref "origin/${GITHUB_BASE_REF}" --to-ref HEAD
          else
            uv run pre-commit run --all-files
          fi

  compose-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    needs: [lint, test, migration-roundtrip-test]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.12"
      - run: uv sync --frozen --extra dev
      - name: Compose hub + vault minimal smoke
        run: .venv/bin/python -m pytest tests/integration -q -m "compose_hub or compose_vault"
      - name: Hub Litestream + MinIO smoke
        run: .venv/bin/python -m pytest tests/integration/test_litestream_smoke.py -q -m compose_litestream
      - name: Vault r2-pull worker integration
        run: .venv/bin/python -m pytest tests/integration/test_r2_pull.py -q -m compose_r2_pull
      - name: Hub default profile (Prometheus + Grafana)
        run: .venv/bin/python -m pytest tests/integration -q -m compose_observability
