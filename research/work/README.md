# work/

Scratch space for **non-canonical** extraction attempts (debug scripts output, one-off trials).

**Default:** contents here are **not** part of the reviewed artifact trail—prefer promoting finished layer outputs into `extracted/<document_id>/` with manifest updates.

If something here must be committed, record why in `catchup.md` or the document manifest `notes` field.
